;;; mshe-standalone.dai --- Models to use with satndalone Daisy.
;;
;; Use the `mshe' models below instead of `default' in your setup
;; files, then change the
;;   (load file "mshe-standalone.dai")
;; to
;;   (load file "mshe.dai")
;; to use the same setup files in a coupled simulation.

(defchemical mshe default)

(defcolumn mshe default)

;;; mshe-standalone.dai ends here.
